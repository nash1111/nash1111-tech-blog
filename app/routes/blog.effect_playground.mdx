---
title: EffectTSのPlaygroundが出てました
description: EffectTSのPlaygroundが出てました
published: 2024-07-31
tags: ["TypeScript"]
thumbnail: /blogthumbnails/13.jpg
---

## 結論
[EffectTS Playground](https://effect.website/play)が出ており、これはとても便利です  
また、[EffectTSのドキュメント](https://effect.website/docs)がとても充実しているため、EffectTSの学習にはとても有用です

## 方法
ブランチがblog-postで始まる場合はレビュー不要としてアクションを行わないようにしました
```
jobs:
  pr_agent_job:
    if: ${{ github.event.sender.type != 'Bot' && !startsWith(github.head_ref, 'blog-post') }}
```